/*
* 开头的符号和e、E后面的符号可能有可能没有
* 有e或者E后面必定跟着一个数字
* js小数点前后必须跟着一个值，比如12.表示12，.12表示0.12
* */
//带符号的整数、浮点数、科学计数法
let reg = /[+-]?(\d+([.]\d*)|\d*([.]\d+))([eE][+-]?\d+)?/g
//二进制
let reg1 = /^0[bB][01]+/g
//八进制
let reg2 = /^0[Oo]?[0-7]+/
//16进制
let reg3 = /^0[xX][0-9a-fA-F]+/
let str = '.1.2a545c.333ac.2b.3.3'
console.log(str.match(reg));